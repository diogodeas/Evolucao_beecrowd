#include <stdio.h>
#include <string.h>

int main(){
    char entradas[10][31];
    int i, j;
    for(i=0; i<10; i++){
        scanf("%s", &entradas[i]);
        getchar(); 
    }
    for(j=0; j<10; j++){
        if(j==2)
            printf("%s\n", entradas[j]);
        else if(j==6)
            printf("%s\n", entradas[j]);
        else if(j==8)
            printf("%s\n", entradas[j]);
    }
    return 0;
}
